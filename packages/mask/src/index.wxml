<view
    style="{{maskStyle}}"
    class="modo-mask {{maskStatus ? 'is-show' : ''}}"
    catch:tap="handleTapMask"
    catch:touchmove="noop">
</view>
<slot
    name="content"
    bind:tap="handleTapMask"
    catch:touchmove="noop">
</slot>
